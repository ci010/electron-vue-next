{
  "name": "create-electron-vue-next-app",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "dev:docs": "node scripts/dev.docs.js",
    "dev": "npm run dev --prefix electron-vue-next",
    "build": "npm run build --prefix electron-vue-next",
    "build:docs": "vitepress build docs",
    "build:dir": "npm run build:dir --prefix electron-vue-next",
    "build:lite": "npm run build:lite --prefix electron-vue-next",
    "build:production": "npm run build:production --prefix electron-vue-next",
    "lint": "eslint --ext .ts,.vue,.js --config electron-vue-next/.eslintrc.js electron-vue-next/src electron-vue-next/scripts index.js scripts",
    "lint:fix": "npm run lint -- --fix"
  },
  "author": {
    "email": "cijhn@hotmail.com",
    "name": "ci010"
  },
  "license": "MIT",
  "workspaces": [
    "electron-vue-next"
  ],
  "dependencies": {
    "electron-vue-next": "^0.0.0"
  },
  "devDependencies": {
    "vitepress": "^0.7.4"
  }
}
